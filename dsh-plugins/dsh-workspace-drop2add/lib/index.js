/**
 * Host half for the workspace-drop2add experiment.
 *
 * The browser plugin uses DSH's existing workspace controller, so it neither
 * opens a second listener nor adds a new server API.
 */
export function apply() {}
