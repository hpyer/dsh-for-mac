# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的记录方式。

## [0.3.5] - 2026-09-12

### 修复

- 为 macOS 13 的旧版 WebKit 补充 `Iterator` 兼容实现，修复 DSH 0.1.5-rc.1 文档预览插件导致整个插件树加载失败的问题，并同步兼容其 PDF Worker。

## [0.3.4] - 2026-09-08

### 修复

- 兼容新版 pnpm 的构建脚本配置，允许并重建 DSH 所需原生依赖，修复 DSH 0.1.3-alpha.2 因缺少 fs_ext.node 无法启动的问题。
- 重新启动 DSH 时优先使用设置中选择的版本；启动失败后可退回上一可用版本，并同步更新版本选择。
- 收集启动进程的标准输出与错误输出，在启动失败时显示具体原因，并优化启动状态提示。

## [0.3.3] - 2026-09-08

### 新增

- 设置顶部显示 DshForMac 版本号，并在 DSH Market 右侧提供 GitHub 仓库链接。

### 调整

- 受管 DSH 版本默认保留数量从 3 个增加至 5 个，稳定版与预发布版合并计数；当前运行或用户固定的版本不在其中时额外保留。

### 修复

- 应用持续运行、重新激活或系统唤醒后，按设置的每天、每周或每月间隔检查 DSH 更新。
- 发现 DSH 新版本后立即显示工具栏提示，设置中点击“下载”链接才开始下载；支持保留待下载状态和失败后重试。

## [0.3.2] - 2026-09-04

### 修复

- 兼容 DSH 0.1.2-rc.1 的工作区文件打开 RPC，恢复对话内链接、思考链和相对路径产物的应用内预览。
- 支持在应用内预览 SQL 文件，并覆盖 Markdown、Java、Vue 等文本产物链接的回归场景。

## [0.3.1] - 2026-09-03

### 修复

- 保留检测到的 pnpm 可执行文件路径，确保 DSH 运行时安装继续使用实际可用的 pnpm。

## [0.3.0] - 2026-09-03

### 新增

- 新增本地 `dsh-workspace-drop2add` 插件：可将 Finder 文件夹拖入 DSH 左侧栏来创建工作区，右侧区域仍按原逻辑添加对话附件。
- 在设置中统一管理 DSH Market 与文件夹拖放推荐插件；插件状态直接同步 DSH `web` profile，保存后可选择立即重启应用变更。
- DMG 内置文件夹拖放插件；应用安装或更新后会自动将旧本地链接迁移到当前应用资源，避免开发目录或旧 App 路径失效。

### 修复

- 限定对话附件拖放遮罩至右侧内容区，避免文件夹拖入左侧栏时误触发图片上传。
- 统一文件夹拖放遮罩与 DSH 附件拖放的视觉反馈，并在工作区添加成功后显示轻提示。

## [0.2.0] - 2026-09-02

### 新增

- 更新检查始终包含 `latest`，并可额外启用 `alpha`、`beta` 或 `next` 标签；按完整 SemVer 优先级选择可用的新版本。
- 在工具栏提示已下载但尚未运行的新 DSH 版本，并在设置中显示检查与下载进度。
- 启动失败时识别插件导出不兼容，并说明具体插件、模块和缺失导出。

### 修复

- 切换已下载版本后，立即重启会启动所选版本。
- 兼容新版 DSH 的一次性本机 Web 认证 URL，避免 WebView 被拒绝访问。
- 由 DshForMac 接管 dshmarket 的插件重启请求，避免与 dshmarket 自行拉起的进程发生竞争。
- 改善 pnpm 安装后的完整性校验和错误摘要，避免可用版本被误报为安装失败。

## [0.1.1]

### 修复

- 为 macOS 13 及更早系统的内嵌 WebView 补充 `AbortSignal.any` 和 `AbortSignal.timeout` 兼容实现，修复新版 DSH 页面无法加载的问题。

## [0.1.0] - 初始版本

### 新增

- 提供原生 AppKit macOS 应用，将 DeepSeek Harness（DSH）作为本地 Web 服务嵌入应用窗口。
- 检测本机 Node.js、npm 与 npx，并支持从 `PATH`、Homebrew 及常见 Node 版本管理器路径查找；可手动选择 `node` 可执行文件。
- 校验 Node.js 兼容性：支持 v22.19+（限 v22）和 v24+。
- 自动下载、安装并完整性校验 `@deepseek-ai/dsh` 的精确版本；优先使用 pnpm，必要时尝试通过 Corepack 启用 pnpm，并可回退使用 npm。
- 为每个 DSH 版本创建独立运行时目录，并仅在本地健康检查成功后更新当前版本软链接；自动保留最多三个版本，同时保护当前和用户选定的版本。
- 使用 `127.0.0.1` 本地地址启动 DSH，默认端口为 3080，支持在设置中配置 1–65535 端口范围。
- 提供 DSH 版本选择、更新检查频率、立即检查更新、下载镜像选择和已安装版本目录入口。
- 提供菜单栏状态图标、运行状态指示、一键重启 DSH、重新加载 DSH 页面和应用内设置窗口。
- 支持在右侧只读预览 DSH 产出的文本、Markdown、图片和 SVG 文件，并可将不受支持的文件交给默认应用打开。
- 提供 Intel 与 Apple Silicon 通用二进制的未签名 DMG 打包脚本。

### 安全与行为

- DSH 由已检测到的 Node.js 直接启动，不通过 shell 拼接命令，也不会自动打开系统浏览器。
- 内嵌 WebView 仅加载配置端口的 `http://127.0.0.1` 服务；其他链接和文件须经用户确认后打开。
- 不捆绑、静默安装或修改用户的 Node.js、npm、pnpm 与全局配置；npm registry 仅通过 DshForMac 子进程环境变量传入。
- 不设置 `DSH_HOME`，保留 DSH 上游默认用户数据路径，以便复用既有配置、会话和插件。
